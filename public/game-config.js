// Tunable gameplay timing, shared by app.js (single-player) and
// multiplayer.js. Change values here rather than hunting through both
// game loops.
window.REPLICO_GAME_CONFIG = {
  RECORD_DURATION_MS: 2000, // how long the mic records each attempt
};
